
var rest = require('restler'),
    Q = require('q');

module.exports = apiRouter;

function apiRouter(message){

    var uri = message.uri;

    switch (uri) {
        case '/api/v1/current_bridge/bridge':

    }

}
